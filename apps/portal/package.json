{
  "private": true,
  "scripts": {
    "setup:client": "bash setup_client.sh",
    "deploy:all": "bash deploy_all.sh",
    "deploy:some": "bash deploy_some.sh",
    "deploy:client": "bash deploy_client.sh",
    "bump:version": "bash bump_version.sh"
  },
  "devDependencies": {
    "simple-git-hooks": "^2.11.1",
    "lint-staged": "^15.2.0"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "pwa-client/**/*.{ts,vue}": "bash -c 'cd pwa-client && npx vitest run'"
  }
}
